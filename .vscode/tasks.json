{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cffi compile",
            "detail": "Compiles C code using cffi into .dll or .so files",
            "type": "shell",
            "command": "python ${workspaceFolder}/src/py_extension_build.py"
        }
    ]
}